name: docs

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-22.04
    env:
      IMAGE: hypern-docs:latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.docs
          tags: ${{ env.IMAGE }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save image
        run: docker save ${{ env.IMAGE }} | gzip > hypern-docs.tar.gz

      - name: Copy image to server
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.DOCS_HOST }}
          username: ${{ secrets.DOCS_USER }}
          key: ${{ secrets.DOCS_SSH_KEY }}
          port: ${{ secrets.DOCS_PORT || 22 }}
          source: "hypern-docs.tar.gz"
          target: "~/deploy/"

      - name: Copy compose to server
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.DOCS_HOST }}
          username: ${{ secrets.DOCS_USER }}
          key: ${{ secrets.DOCS_SSH_KEY }}
          port: ${{ secrets.DOCS_PORT || 22 }}
          source: "deploy/docker-compose.yml"
          target: "~/deploy/"
          strip_components: 1

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.2.5
        with:
          host: ${{ secrets.DOCS_HOST }}
          username: ${{ secrets.DOCS_USER }}
          key: ${{ secrets.DOCS_SSH_KEY }}
          port: ${{ secrets.DOCS_PORT || 22 }}
          command_timeout: 5m
          script: |
            set -euo pipefail
            cd ~/deploy
            docker load -i hypern-docs.tar.gz
            rm -f hypern-docs.tar.gz
            docker compose up -d --force-recreate --no-deps docs
            docker image prune -f
            docker compose ps
